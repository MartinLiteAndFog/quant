# Webhook + dashboard (Railway etc. often run: pip install -r requirements.txt)
fastapi>=0.110
uvicorn>=0.27
python-dotenv>=1.0
pandas>=2.0
numpy>=1.24
pydantic>=2.0
rich>=13.0
# Install this package so "quant.execution.webhook_server" is importable
.
